import 'package:flutter/material.dart';

class AppConstants {
  const AppConstants._();

  static const pauseNumber = 2;

  // Progress bar appearance
  static const progressBarHeight = 20.0;
  static const progressBarBorderRadius = 8.0;
  static const strokeWidth = 15.0;

  //TODO: to theme
  static const stripePrimaryColor = Colors.blue;
  static const stripeSecondaryColor = Colors.lightBlueAccent;
  static const barBackgroundColor = Colors.grey;
}
